import React from 'react';

/**
 * Settings component.
 * Currently a placeholder for future settings configuration.
 *
 * @returns {JSX.Element} The rendered settings page.
 */
export const Settings = () => {
  return <div><h1>Ustawienia</h1></div>;
};
